using System;
using Systems.Collections.generic;
public class AddTwoIntegers
{
    public AddTwoIntegers()
    {
        // Given the head pointers of two linked lists where each linked list represents an integer number (each node is a digit), add them and return the resulting linked list.
        LinkedList<int> NumberOne = new() { 1, 2, 3, 4, 5, 6, 7, 8 };
        LinkedList<int> NumberTwo = new() { 1, 4, 9, 2 };
        LinkedList<int> NumberSum = new() { };
        AddTwoIntegers()
            { }

        void AddItUp()
        {
            int last => NumberOne.count > NumberTwo.count ? NumberOne.count : NumberTwo.count;
            NumberSum = NumberOne.count = last ? NumberOne : NumberTwo;
            
        }
        int last => NumberOne.length > NumberTwo.length ? NumberOne.length : NumberTwo.length;
        for (int i; i < last; i++)
        {

        }
        LinkedList<int> AddItUp()
        void Display(LinkedList<int> linkedList)
        {
            Console.WriteLine();
            foreach (int digit in linkedList)
            {
                Console.Write(digit);
            }
            Console.WriteLine();
        }
    }
}